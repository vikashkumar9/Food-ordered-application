/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost"], // Specify the domains from which images will be served
  },
  reactStrictMode: false,
};

export default nextConfig;
